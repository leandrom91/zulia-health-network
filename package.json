{
  "name": "zulia-health-network",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "vercel-build": "npm install --include=dev --prefix server && npm install --include=dev --prefix client && npm run build --prefix client"
  }
}
