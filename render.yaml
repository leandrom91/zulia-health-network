services:
  - type: web
    name: zulia-health-api
    env: node
    rootDir: server
    buildCommand: npm install --include=dev && npm run build
    startCommand: npm start
    envVarGroups: []
    envVars:
      - key: NODE_ENV
        value: production
